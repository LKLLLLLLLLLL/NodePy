[project]
name = "nodepy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "invoke>=2.2.1",
]

[project.scripts]
dev = "scripts.build:start_dev"
prod = "scripts.build:start_prod"
build = "scripts.build:build"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scripts"]
